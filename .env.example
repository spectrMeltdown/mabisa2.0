# Copy to .env and adjust for your environment (e.g. `cp .env.example .env`).
# The .env file is git-ignored and holds local database credentials.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=mabisa2.0
DB_USER=mabisa
DB_PASS=mabisa
