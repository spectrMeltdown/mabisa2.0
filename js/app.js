// TODO: find a way to hide the "/Admin/" part of the url. Below works, however, pressing back in the browser takes it to a wrong page
// window.addEventListener("load", () => {
//   let currentURL = window.location.pathname;
//   if (currentURL.includes("/pages/")) {
//     let newURL = currentURL.replace("/pages/", "/");
//     history.replaceState(null, null, newURL);
//   }
// });
